{
  "dependencies": {
    "axios": "^1.6.8",
    "concurrently": "^8.2.2",
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0",
    "node-sass": "^9.0.0"
  },
  "scripts": {
    "start": "json-server-auth --watch db.json --port 8080"
  }
}
